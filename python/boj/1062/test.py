#a,n,t,c,a가 무조건 있어야 하고, 단어는 N개밖에 없을 때 단어의 최댓값 
# a.n,t,c,a는 무조건 포함해야 하므로 string 에서 삭제 => 삭제하는건 set으로 하기!1
from itertools import combinations

except_char = set(['a','c',i'n't])
words = [input list - except_char for _ in range(N)]

alpha = set( sum([list(word) for word in words ], []) )

comb = combinations(alpha, min( len(alpha), K-5) )

for comb in combs:
 	comb = set(comb):
		if w
		 remove(값), pop(idx)

from string import ascii_lowercase

#set(a), set(b)로 비교 후 if a <= b: A의 요소가 B에 포함되는지
아
