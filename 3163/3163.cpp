#include<iostream>
#include<queue>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int T;
	cin >> T;
	while(T--) {
		int N,L,K;
		cin >> N >> L >> K;
		vector<int> id;
		vector<int> loc;

		for(int i=0;i<

	}

	return 0;
}
